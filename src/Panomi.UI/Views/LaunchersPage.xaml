<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Panomi.UI.Views.LaunchersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Panomi.UI.Views"
    Background="Transparent">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock 
                Text="Launchers" 
                Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock 
                Text="Detected game launchers on your system"
                Style="{StaticResource BodyTextBlockStyle}"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Launchers List -->
        <ListView 
            x:Name="LaunchersList"
            Grid.Row="1"
            ItemsSource="{x:Bind Launchers, Mode=OneWay}"
            SelectionMode="None">
            
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Padding="12" ColumnSpacing="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="8" Margin="0,4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="48"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <!-- Launcher Icon -->
                        <Border 
                            Grid.Column="0" 
                            Width="48" 
                            Height="48" 
                            CornerRadius="8"
                            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}">
                            <FontIcon 
                                Glyph="&#xE7FC;" 
                                FontSize="20" 
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        </Border>

                        <!-- Launcher Info -->
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock 
                                Text="{Binding Name}" 
                                Style="{StaticResource BodyStrongTextBlockStyle}"/>
                            <TextBlock 
                                Text="{Binding StatusText}" 
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Foreground="{Binding StatusColor}"/>
                        </StackPanel>

                        <!-- Game Count -->
                        <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right">
                            <TextBlock 
                                Text="{Binding GameCount}" 
                                Style="{StaticResource SubtitleTextBlockStyle}"
                                HorizontalAlignment="Center"/>
                            <TextBlock 
                                Text="games" 
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                HorizontalAlignment="Center"/>
                        </StackPanel>

                        <!-- Scan Button -->
                        <Button 
                            Grid.Column="3" 
                            Content="Scan"
                            Click="ScanLauncher_Click"
                            Tag="{Binding Id}"
                            IsEnabled="{Binding IsInstalled}"
                            VerticalAlignment="Center"
                            Margin="16,0,0,0"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
